export type FileConfig = {
  endpoint: string;
  accessKeyId: string;
  secretAccessKey: string;
  defaultMinioBucket: string;
  region: string;
  publishURL: string;
  maxFileSize: number;
  fileDomain: string;
};
